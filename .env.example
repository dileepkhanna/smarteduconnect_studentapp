# =========================
# ASE Parent/Student App
# =========================
#
# Local backend (web/desktop): localhost
# For Android emulator use: http://10.0.2.2:3000
# For real device use your PC LAN IP: http://192.168.x.x:3000
BASE_URL=http://localhost:3000
# Optional fallback host (leave empty in production to avoid localhost failover)
# BASE_URL_FALLBACK=http://10.0.2.2:3000
API_PREFIX=api
FLAVOR=dev
ENABLE_NETWORK_LOGS=true
APP_NAME=ASE School Parent
